import TemplateTable from '@/components/dashboard/cron/TemplateTable';

/** Template-message monitoring page — per-lead template send tracking. */
export default function TemplatePage() {
  return <TemplateTable />;
}
